# Trenx Flutter — Project Handover State

**Generated:** 2026-06-30T21:17 (ICT)  
**Working directory:** `d:/Trenx_Flutter/`  
**Previous conversation ID:** `ea4c5e5e-06b2-4b3a-902e-85965fe0ff2d`

---

## 1. Current State & Completed Tasks

### ✅ Scaffolding & Project Setup
| Item | Status | Detail |
|------|--------|--------|
| Flutter project created | ✅ Done | `d:/Trenx_Flutter/` — valid Flutter SDK `^3.10.4` project |
| `pubspec.yaml` | ✅ Done | Dependencies: `shared_preferences ^2.2.0`, `flutter_svg ^2.0.7`, `cupertino_icons ^1.0.8`; dev: `integration_test`, `flutter_lints ^6.0.0` |
| `pubspec.lock` | ✅ Done | Lock file present (13 767 bytes) — `flutter pub get` has been run |
| `analysis_options.yaml` | ✅ Done | Lints config present |
| Asset declaration | ✅ Done | `assets/exercise_placeholder.png` declared in `pubspec.yaml` |
| Localization infra | ✅ Done | `GlobalMaterialLocalizations` wired in `main.dart`; `flutter_localizations` SDK dep referenced |

### ✅ Directory Structure
```
lib/
├── main.dart                  ← Central navigator / state machine (16 994 bytes)
├── data/
│   ├── api/                   ← (empty, ready for data-source classes)
│   └── repositories/          ← (empty, ready for repo classes)
├── domain/                    ← (empty, ready for models / use-cases)
└── presentation/
    ├── screens/
    │   └── splash_screen.dart ← Animated splash with onTimeout + onGetStarted callbacks
    └── widgets/               ← (empty, ready for shared widgets)
```

### ✅ `main.dart` — Navigation State Machine
`_MyAppState` uses plain `setState` to navigate across all required screens:
- Splash → Onboarding → Login → Register → Dashboard → Explore → Feed → Profile  
- Exercise Detail → Workout Detail → Workout Player → Workout Summary → Settings

All screen stubs are inline in `main.dart` (as placeholder `Scaffold` + `Text` widgets).  
Language switching (VI / EN) is wired but locale strings are not yet extracted to ARB/JSON.

### ✅ Assets
- `assets/exercise_placeholder.png` — generic dumbbell image generated by `imagegen-frontend-mobile`.  
- **No real per-exercise images yet.**

### ✅ Android Native Fixes (in the old `d:/Trenx` project — done before the Flutter rewrite decision)
- Removed "BY NGUYEN CONG TRU" credit from splash.
- Fixed black-text-on-black-background bug in `DifficultyUiHelper.java` / `item_exercise.xml`.
- Replaced Community tab placeholder data with realistic entries.
- Wrote `exercises_en.json` + `exercises_vi.json` with 15+ real exercises, detailed instructions, and GIF references.
- Fixed `ApiClient.java` to load localized JSON from raw resources.

---

## 2. Next Immediate Steps

Work **in this order** — each step unblocks the next.

### Step 1 — Verify the build baseline (15 min)
```bash
cd d:/Trenx_Flutter
flutter pub get
flutter analyze          # must return 0 issues
flutter build apk        # must succeed with 0 warnings
```
Fix any issues `flutter analyze` surfaces before proceeding.

### Step 2 — Extract localisation strings (1–2 h)
- Create `lib/l10n/app_en.arb` and `lib/l10n/app_vi.arb` with every UI string from `main.dart`.
- Add `flutter_localizations` and `intl` to `pubspec.yaml` and enable code-gen (`generate: true` in `pubspec.yaml`).
- Replace every hardcoded string in `main.dart` and `splash_screen.dart` with `AppLocalizations.of(context)!.xxx`.

### Step 3 — Build real screen UIs (3–4 h per screen, priority order)
Work screen by screen, extracting each stub out of `main.dart` into its own file under `lib/presentation/screens/`:

1. **`dashboard_screen.dart`** — hero header, today's workout card, exercise category grid.
2. **`explore_screen.dart`** — search bar + filtered exercise list.
3. **`exercise_detail_screen.dart`** — exercise name, animated GIF/image, full step-by-step instructions, muscle diagram.
4. **`workout_player_screen.dart`** — countdown timer, rep counter, exercise transitions.
5. **`workout_summary_screen.dart`** — calories burned, time elapsed, exercises completed.
6. **`feed_screen.dart`** (Community) — real post cards with user avatar + content.
7. **`profile_screen.dart`** — user stats, badges, history.
8. **`settings_screen.dart`** — language toggle (EN/VI), notification prefs, theme toggle.
9. **`onboarding_screen.dart`** — 3-slide goal selector (weight loss / conditioning / bodybuilding).
10. **`login_screen.dart`** + **`register_screen.dart`** — form screens.

### Step 4 — Generate exercise images (run while coding)
Use the **`imagegen-frontend-mobile`** skill to generate one illustration per exercise category:
- `assets/images/squat.png`, `pushup.png`, `deadlift.png`, `plank.png`, `lunges.png`, `burpee.png`, `pull_up.png`, `crunch.png`, `bicep_curl.png`, `shoulder_press.png` (minimum set).
- Declare each new asset in `pubspec.yaml` under `assets/images/`.

### Step 5 — Exercise data layer (2 h)
- Create `lib/domain/models/exercise.dart` — `Exercise` value object.
- Create `lib/domain/models/workout_plan.dart` — `WorkoutPlan` value object.
- Create `lib/data/repositories/exercise_repository.dart` — loads from bundled JSON assets.
- Create `assets/data/exercises.json` — a single structured file with ≥ 30 exercises covering all three goal categories (weight loss, conditioning, bodybuilding), each with:
  - `id`, `nameEn`, `nameVi`, `category`, `muscles`, `equipment`
  - `stepsEn` (list of strings), `stepsVi` (list of strings)
  - `durationSeconds`, `reps`, `sets`, `imageAsset`, `caloriesPerMinute`

### Step 6 — Final polish & audit (1 h)
- Run `ponytail-review` on `lib/` and remove any identified over-engineering.
- Run `flutter analyze` again — must be clean.
- Run `flutter build apk --release` — must succeed.
- Install on device: `adb install -r build/app/outputs/flutter-apk/app-release.apk`
- Verify: splash → dashboard loads exercises → tap exercise → detail screen shows steps.

---

## 3. Strict Development Rules — Non-Negotiable

> **READ BEFORE WRITING A SINGLE LINE OF CODE**

| Rule | Detail |
|------|--------|
| **No comments** | Absolutely zero `//`, `/* */`, or `///` doc comments in any `.dart` file unless the next agent receives an explicit user instruction to add one. All names must be self-explanatory. |
| **SQA standard** | Every file must pass `flutter analyze` with 0 issues. No `ignore:` annotations. No dead code. No unused imports. |
| **ponytail** | Apply the ponytail skill at every decision point: YAGNI, no abstractions unless explicitly requested, stdlib/platform API first, fewest files possible, shortest working diff. |
| **codegraph** | Variable, function, and class names must be precise enough to understand the call graph without comments. |
| **imagegen-frontend-mobile** | Use this skill for every exercise illustration. Do NOT use stock URLs or placeholder text boxes as permanent assets. |
| **No extra dependencies** | Do not add packages to `pubspec.yaml` unless there is zero stdlib/platform equivalent and the user approves. |
| **No boilerplate** | No generated Bloc/Provider boilerplate unless the user requests it. Use `setState` or `ValueNotifier` — whichever is simpler for the scope. |
| **Fewest files** | Do not create a separate file until a single file exceeds ~300 lines or is reused from 2+ places. |

---

## 4. Key File Paths

| File | Purpose |
|------|---------|
| `d:/Trenx_Flutter/pubspec.yaml` | Dependency & asset manifest |
| `d:/Trenx_Flutter/lib/main.dart` | Central navigator, app entry point, all screen stubs |
| `d:/Trenx_Flutter/lib/presentation/screens/splash_screen.dart` | Animated splash |
| `d:/Trenx_Flutter/assets/exercise_placeholder.png` | Generic exercise image (to be replaced per exercise) |
| `d:/Trenx_Flutter/analyze.log` | Last analysis run output |
| `C:/Users/2601m/.gemini/antigravity/brain/ea4c5e5e-06b2-4b3a-902e-85965fe0ff2d/implementation_plan.md` | Full implementation plan artifact |
| `C:/Users/2601m/.gemini/antigravity/brain/ea4c5e5e-06b2-4b3a-902e-85965fe0ff2d/task.md` | Completed task checklist |

---

## 5. User Context

- **User goals:** fitness app for three audiences — weight loss, general conditioning, bodybuilding.
- **Language:** Vietnamese-first UI, with English as secondary; both languages must be complete.
- **Design bar:** "sướng tay, sướng mắt" — premium feel, smooth 60 fps animations, dark mode.
- **Quota note:** The primary agent hit quota limits (`RESOURCE_EXHAUSTED 429`). If quota is exhausted again, switch model to **GPT-OSS 120B (Medium)** and continue without stopping.
- **No approval needed for next steps:** The user explicitly stated the next agent should proceed automatically through all remaining steps without waiting for prompts.
